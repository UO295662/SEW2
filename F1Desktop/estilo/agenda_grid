
/* Noticias container (sección principal) */
main > section:nth-of-type(0) article {
    margin: 0; /* Eliminar márgenes del contenedor */
}

/* Título de la noticia */
main > section:nth-of-type(0) article h4 {
    margin: 0 0 0.5rem; /* Solo margen inferior */
    font-size: 1.4rem;
    color: #222;
}

/* Entradilla de la noticia */
main > section:nth-of-type(0) article p:nth-of-type(1) {
    margin: 0.5rem 0; /* Ajusta margen superior e inferior */
    font-size: 1rem;
    color: #555;
}

/* Autor */
main > section:nth-of-type(0) article p:nth-of-type(2) {
    margin: 0; /* Eliminar espacio arriba del autor */
    font-size: 0.9rem;
    color: #777;
    font-style: italic;
}

/* Línea horizontal */
main > section:nth-of-type(0) article hr {
    margin: 1rem 0; /* Ajusta espacio antes y después del <hr> */
    border: 0.1rem solid #ccc;
}